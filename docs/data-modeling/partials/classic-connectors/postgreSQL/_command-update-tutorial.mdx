For an automatically detected command, you can update the command metadata.

```ddn title="Use the DDN CLI to introspect your PostgreSQL database:"
ddn connector introspect my_pg
```

```ddn title="Show the resources discovered from your PostgreSQL database:"
ddn connector show-resources my_pg
```

```ddn title="Then, update your commands:"
ddn command update my_pg "*"
```
